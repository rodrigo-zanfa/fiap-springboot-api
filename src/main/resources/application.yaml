server:
  port: 8080

build.name: @project.name@
build.version: @project.version@

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/abctech
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true